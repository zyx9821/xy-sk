name = "tron-monitor"
main = "src/index.js"
compatibility_date = "2026-05-20"

# 定时任务：每 1 分钟执行一次链上巡检
[triggers]
crons = ["* * * * *"]

# 绑定 KV 数据库
[[kv_namespaces]]
binding = "kv"
id = "填入你生成的_KV_ID"

# 绑定 D1 数据库
[[d1_databases]]
binding = "db"
database_name = "db"
database_id = "填入你生成的_D1_ID"
